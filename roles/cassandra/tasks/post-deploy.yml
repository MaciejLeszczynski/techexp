- name: Setting up firewall rules
  become: true
  become_user: root
  firewalld:
    service: https
    permanent: true
    state: enabled

- name: Setting up firewall rules
  become: true
  become_user: root
  firewalld:
    service: http
    permanent: true
    state: enabled

- name: Setting up firewall rules
  become: true
  become_user: root
  firewalld:
    port: 8081/tcp
    permanent: true
    state: enabled

- name: Setting up firewall rules
  become: true
  become_user: root
  firewalld:
    port: 161-162/udp
    permanent: true
