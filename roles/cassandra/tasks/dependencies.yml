- name: Upgrade all packages, excluding kernel
  yum:
    name: '*'
    state: latest
    exclude: kernel*

- name: installing some dependencies
  yum: 
    package: "{{ item }}"
    state: installed 
  loop:
  - vim
  - jna
  - sysstat
  - libselinux-python
  - lsof
