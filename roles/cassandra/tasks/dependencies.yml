- name: Upgrade all packages, excluding kernel
  become: true
  yum:
    name: '*'
    state: latest
    exclude: kernel*

- name: installing some dependencies
  become: true
  yum:
    package: "{{ item }}"
    state: installed
  loop:
  - vim
  - jna
  - sysstat
  - libselinux-python
  - lsof
